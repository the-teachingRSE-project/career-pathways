# This is a basic workflow to help you get started with Actions

name: build-pdf

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      - name: Install system packages
        run: sudo apt-get install ghostscript 
  
      - uses: actions/cache@v4
        with:
          path: | 
            /home/runner/.cargo/bin/
            .texenv
          key: frisket-${{hashFiles('texproject.toml')}}

      - name: Install and setup frisket
        run: |
          cargo install --git https://www.github.com/frisket-tool/frisket
          frisket setup
          frisket install 2026

      - name: Setup local texenv
        run: |
          frisket init --tree-only .
          frisket instantiate

      - name: build pdf
        run: frisket build

      - uses: actions/upload-artifact@v7.0.1 
        with: 
          name: pdf-output
          path: career-pathways.pdf
